apiVersion: 1

providers:
  - name: 'Biometria POC'
    orgId: 1
    folder: 'Vivo — Biometria'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
